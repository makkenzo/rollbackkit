---
"@rollbackkit/core": minor
"@rollbackkit/postgres": minor
"@rollbackkit/cli": patch
---

Harden runtime boundaries with transactional action execution, explicit side effect and conflict recorders, checksum-validated PostgreSQL migrations, and CLI migration helpers delegated through the PostgreSQL package.
